{
  "$comment": "Files under `files` are exempt from the file-length check up to their recorded line count (plus a 10% growth buffer). Each entry is {\"lines\": N, \"reason\": \"…\"}: the reason is mandatory and says why this file gets to be long, or starts with `TODO: ` when it should be split and the reason says how. The check shrink-wraps the numbers on local runs (dead entries and files back under the threshold are removed, and entries with more than 10% slack are ratcheted down), so a reason is the only part written by hand. Files under `exempt` never fail (generated files whose length is not actionable); each needs a reason too. See `.claude/rules/file-length-allowlist.md`.",
  "exempt": {
    "apps/desktop/src-tauri/src/intl/native_strings.gen.rs": "generated by apps/desktop/scripts/gen-native-strings.ts; line count tracks the shipped locale x key matrix, not hand-written code",
    "apps/desktop/src-tauri/src/ipc.rs": "append-only IPC registry (one line per command in `collect_commands!` / `invoke_handler`); line count tracks the command surface, not splittable logic",
    "apps/desktop/src/lib/intl/keys.gen.ts": "generated by scripts/gen-message-keys.js; line count tracks the catalog key count, not hand-written code",
    "apps/desktop/src/lib/ipc/bindings.ts": "generated by tauri-specta; line count tracks the IPC surface, not hand-written code",
    "scripts/check/checks/registry.go": "append-only check registry (one CheckDefinition entry per check); line count tracks the number of checks, not splittable logic"
  },
  "files": {
    "apps/desktop/src-tauri/src/file_system/listing/caching.rs": {
      "lines": 903,
      "reason": "TODO: three concerns share one file and the test siblings already name the seams. Move the full-refresh machinery (`RefreshSlot`, `REFRESH_SLOTS`, `refresh_slot`, `spawn_full_refresh`, `notify_full_refresh`, `notify_full_refresh_locked`, `publish_replacement`, `find_listings_under_path_on_volume`, `refresh_archive_listings`, roughly lines 540-800) into `caching_refresh.rs` at about 260 lines, and the freshness oracle `try_get_authoritative_listing` with its long contract doc into `caching_oracle.rs` at about 105 lines, matching the `caching_replacement_test.rs` and `caching_oracle_test.rs` siblings that already test exactly those two. What stays is about 550 lines of listing lookups (`snapshot_listings`, `find_listings_for_path*`, `find_listings_on_volume`), the cache patch helpers (`insert_entry_sorted`, `remove_entries_by_paths`, `update_entry_sorted`, `carry_forward_tags`, `apply_tags_to_listing`), and the `notify_directory_changed` dispatch with its `notify_added` / `notify_removed` / `notify_modified` arms, which is what the module map in `listing/CLAUDE.md` calls this file."
    },
    "apps/desktop/src-tauri/src/file_system/write_operations/delete/walker.rs": {
      "lines": 942,
      "reason": "TODO: this is two delete engines in one file, sharing imports and nothing else. Split at the banner on line 259: `delete_files_with_progress_inner` (the `walkdir` plus `fs::remove_*` local path, about 230 lines) stays in `walker.rs`, and `VolumeDeleteEntry`, `VolumeScanTracker`, `scan_volume_recursive`, `emit_cancelled_if_aborted`, and `delete_volume_files_with_progress_inner` (about 700 lines) move to `volume_walker.rs`, beside the existing `volume_start.rs` that already holds the volume delete's managed lifecycle for the same reason. `delete/CLAUDE.md` already describes them as parallel paths with `delete_files_start` routing by `volume_id`, and the test siblings are split the same way (`delete_cancel_tests.rs` against the local path, `volume_cancel_tests.rs` and `delete_volume_reuse_tests.rs` against the volume one)."
    },
    "apps/desktop/src-tauri/src/file_system/write_operations/manager.rs": {
      "lines": 1152,
      "reason": "TODO: the admission state machine is buried under vocabulary and facade code that never touches its lock. Move the wire and vocabulary types (`PauseOutcome`, `PauseAllOutcome`, `OperationSummaryText`, `OperationSnapshot`, `OperationsChanged`, about 145 lines of serde and specta structs), the two RAII guards (`ManagedTaskGuard`, `InstantTaskGuard`, about 85 lines), the retained-failure block (`FAILURE_CAPACITY`, `record_failure`, `dismiss_failure`, `dismiss_all_failures`, about 110 lines, whose out-of-band lifetime is the one piece of manager state that outlives a record), and the thin free-function facade at the bottom (`list_operations` through `dismiss_all_failed_operations`, about 95 lines) into children of the existing `manager/` directory. That leaves roughly 750 lines: `ManagerInner`, `OpRecord`, and the `spawn_managed` / `run_admission_pass` / `on_settled` / `free_and_remove` / `cancel_if_queued` machinery that genuinely has to share one `Mutex`."
    },
    "apps/desktop/src-tauri/src/file_system/write_operations/mod.rs": {
      "lines": 801,
      "reason": "This is a facade, not an implementation: 46 `mod` declarations, about 150 lines of individually documented re-exports, and about 85 lines of `#[cfg(test)] #[path]` declarations for 30-plus test siblings account for most of it, all of it append-only registry shape. The only real code is `start_write_operation` (one deferred-start closure, about 165 lines) plus `copy_files_start`, `move_files_start`, `delete_files_start`, and `trash_files_start`, which each log, derive lanes, build a summary, and hand a validation closure to it."
    },
    "apps/desktop/src-tauri/src/file_system/write_operations/transfer/transfer_driver_async_tests.rs": {
      "lines": 1144,
      "reason": "TODO: the pause-gate band is a separate suite wearing the same filename. Lines 952-1172 (`async_driver_parks_while_paused_then_resumes_to_completion` and `async_driver_cancel_while_paused_unblocks_and_cancels`, plus the 15-line comment explaining their per-file `Semaphore` harness) cover the pause gate rather than the driver's transfer loop, run on a four-worker runtime where every other test runs on two, and account for about 230 lines. Move them to `transfer_driver_async_pause_tests.rs`, which follows the split this directory already uses (`transfer_driver_sync_tests.rs`, `transfer_driver_concurrent_tests.rs`, `transfer_driver_pre_skip_tests.rs`), and lift the three closure-shape aliases (`FetchFut`, `ResolveFut`, `TransferFut`) into `transfer_driver_test_support.rs` so both files share them. That leaves about 990 lines covering data safety, conflict resolution, progress accounting, dest derivation, ordering, and the `Send` guard, with the module doc's coverage map trimmed to match."
    },
    "apps/desktop/src-tauri/src/file_system/write_operations/transfer/transfer_probe.rs": {
      "lines": 983,
      "reason": "TODO: the file holds a recorder and the watchdog that reads it, and they part cleanly. Move the watchdog into a `transfer_probe/watchdog.rs` child (a child module still reaches the parent's private fields, the way `mod vocabulary` already works here): `WatchdogState`, `spawn_watchdog`, the `STALL_TICK` / `STALL_AFTER` / `STALL_ABORT_AFTER` / `HEARTBEAT_AFTER_SECS` constants with `stall_abort_after`, `stall_abort_override`, and `StallAbortGuard`, plus an `impl OperationProbe` block carrying `watchdog_step`, `connection_proven_dead`, `restart_task_stillness`, `track_and_abort_wedged_tasks`, and `emit_heartbeat`. That is about 345 lines, most of the file's heaviest prose (the four-guard rationale, the QNAP keepalive false-positive measurement, the `ERR-AYVM4` note) included. What stays is about 570 lines of `TaskProbe`, `OperationProbe`, `TaskProbeHandle`, `OperationProbeGuard`, the `CURRENT_TASK_PROBE` task-local accessors, the `REGISTRY`, and the `render_dump` / `render_live_dump` reporting the incident doc points at."
    },
    "apps/desktop/src-tauri/src/file_system/write_operations/transfer/volume/copy.rs": {
      "lines": 1316,
      "reason": "TODO: the managed entry point and the engine are two jobs. Move `copy_between_volumes` (volume-capability validation, the both-local shortcut that converts a `VolumeCopyConfig` and delegates to the local pipeline, and the `spawn_managed` wiring, about 285 lines) and `scan_for_volume_copy` (the dialog's preview scan, about 95 lines) into a `copy_start.rs` sibling, mirroring `delete/volume_start.rs`, which exists for exactly this reason. What remains is about 930 lines including roughly 80 lines of `#[cfg(test)] #[path]` declarations for 24 suites, and it holds together: `copy_volumes_with_progress` is one phase runner from the dest-inside-source guard through Phase 0.5 create, temp reaping, `DestNameIndex` build, preflight scan, space check, bulk skip, driver call, and the rollback-or-cancel tail, threading about 25 locals end to end. Its two drivers already left for `copy_concurrent.rs` and `copy_serial.rs` behind 25-field and 20-field context structs, so carving out the terminal tail as well would mean a third such struct and read worse than what is there."
    },
    "apps/desktop/src-tauri/src/file_system/write_operations/transfer/volume/copy_concurrency_bench.rs": {
      "lines": 823,
      "reason": "This is one `#[ignore]`d measurement harness with a single entry point, `concurrency_bench_sweep_window_against_wall_clock`, and the file-length check gives it the 800-line source budget only because its name ends in `_bench.rs` rather than `_tests.rs`. About 90 lines are the runbook module doc (how to run it against Docker or the NAS, the nine environment knobs, and why the numbers are shaped the way they are), and about 100 more are the `Volume` trait boilerplate for `FixedConcurrencySource`, the wrapper that sweeps the window by reporting a fixed `max_concurrent_ops`. The rest is fixtures nothing else uses (`Corpus`, `build_corpus`, `Shape`, `DestMode`, `bench_content`, `timed_copy`, `connect_target`, `serial_precheck_floor`, `median`), so splitting would scatter them for no reader's benefit."
    },
    "apps/desktop/src-tauri/src/file_system/write_operations/transfer/volume/strategy_test_support.rs": {
      "lines": 1245,
      "reason": "TODO: split it, because it is 13 unrelated `Volume` / `VolumeReadStream` doubles serving nine suites and the seams show in who reads what. `RecursiveDeleteVolume` and `UndeletableSource` (lines 371-502, ~130 lines) have no `strategy_*` consumer at all; their readers are `cleanup_tests.rs`, `conflict_tests.rs`, and `move_failure_tests.rs`, so they belong in a `cleanup_test_support.rs`. The auto-yield cluster `ReleasingStream` + `ReleasingSource` + `AutoYieldTuningGuard` + `YieldingSource` + `NeverPendingYieldSource` (lines 765-1129, ~365 lines) is read only by `strategy_yield_tests.rs`, `strategy_dest_yield_tests.rs`, and `copy_wedge_test_support.rs`, and should join the existing `strategy_dest_yield_test_support.rs` or get its own `strategy_yield_test_support.rs`. `FlakyDest` (~125 lines) pairs with `strategy_retry_tests.rs`. That leaves the abort and stale-handle doubles (`SlowChunkedStream`, `SlowSource`, `FailOnceStaleDest`, `WedgedThenWorkingDest`, `WedgedOpenSource`, `TierOneWitnessDest`) at ~500 lines here. The header comment still claims four suites, so the file has outgrown its own description, and note that a `_test_support.rs` name gets the 800-line budget, not the 1,200-line test one."
    },
    "apps/desktop/src-tauri/src/file_viewer/rows_test.rs": {
      "lines": 1212,
      "reason": "It is the proof corpus for ONE rule, and splitting it would separate the properties from the fixtures they run over. The file is a reference implementation of the row boundary set brute-forced over a whole file, a corpus of ~40 crafted cases across four encodings, and the property battery that probes every one of them at EVERY offset against that reference, including the cross-check that binds `rows.rs`'s backward reading to `row_walk.rs`'s forward walk. Every property reads most of the corpus and the corpus exists only for the properties, so any cut either duplicates the fixtures or puts them behind a `_test_support.rs` import that hides which case a red assertion came from. The rule itself did split (`rows.rs` / `row_walk.rs`); this file deliberately did not follow, because it is what proves the two halves still agree."
    },
    "apps/desktop/src-tauri/src/file_viewer/session.rs": {
      "lines": 1630,
      "reason": "TODO: split it, because the tests already live in `session_test.rs`, so all 1,630 lines are production code, and four clusters come out whole as children of the existing `session/` directory (which already holds `test_hooks.rs`), keeping access to `ViewerSession`'s private fields. `session/search.rs` takes `SearchState`, `SearchStatus`, `SearchPollResult`, `search_start`, `spawn_search_worker`, `search_poll`, and `search_cancel` (~250 lines, touching only `session.path` and `session.search`). `session/watcher_thread.rs` takes `spawn_watcher_manager`, `catch_up_after_subscribe`, `emit_file_changed`, `handle_watcher_event`, `apply_tail_extend`, `coalesce_pending_grew`, `push_pending_grew`, plus `set_tail_mode` and `reload` (~290 lines). `session/encoding_switch.rs` takes `EncodingChoice`, `EncodingOptions`, `all_encoding_choices`, `get_encoding_options`, and `set_encoding` (~215 lines). `session/open.rs` takes `open_session`, `open_session_as_text`, `open_for_window`, `reusable_temp`, `open_session_inner`, `open_session_core`, and the ByteSeek-to-LineIndex upgrade thread (~290 lines). What stays is the `ViewerSession` type, the `SESSIONS` and `WINDOW_TO_SESSION` registries, and the read path (`get_lines`, `read_range`, `cancel_read`, `close_session`) at ~480 lines. The drain-and-swap protocol stays shared, but it travels through documented fields rather than one function's locals, so no cut runs through a live state machine."
    },
    "apps/desktop/src-tauri/src/file_viewer/session_test.rs": {
      "lines": 1777,
      "reason": "TODO: split it, because 62 tests in five barely-overlapping groups sit 550 lines over the test budget. Move the search tests (`search_start_and_poll` through `test_regex_search_returns_matches`, including the watchdog and invalid-query ones) to `session_search_test.rs` (~400 lines), the `read_range` and `write_range_to_file` block at lines 408-752 to `session_range_test.rs` (~345 lines), the tail-mode and watcher tests (`tail_mode_toggle_persists_on_session` through `test_session_close_stops_watcher`) to `session_tail_test.rs` (~440 lines), and the encoding tests (`get_encoding_options_returns_detected_and_all`, both `set_encoding_*`, `test_append_during_encoding_rebuild_not_dropped`, `test_set_encoding_during_rebuild_serialization`, `test_set_encoding_ascii_compatible_is_instant`) to `session_encoding_test.rs` (~290 lines). The ~70 lines of shared helpers (`create_test_dir`, `write_test_file`, `append_test_file`, `wait_for_watcher_subscribed`, `upgraded_to_line_index`, `literal_mode`) go to a `session_test_support.rs`, the convention the sibling `search_cancel_test_support.rs` already sets, so no fixture scatters."
    },
    "apps/desktop/src-tauri/src/mcp/server.rs": {
      "lines": 1008,
      "reason": "TODO: split it, because it is two halves plus an inline test block. Lines 1-475 are binding and lifecycle (`BindStrategy`, `BindMode`, `BindError`, `resolve_bind_strategy`, `decide_rebind_action`, `bind_listener`, `bind_with_probe`, `bind_ephemeral`, `start_mcp_server`, `rebind_interactive`, `serve_on`, the `stop_mcp_server*` pair, and the port-file statics); lines 476-820 are the Streamable HTTP transport (`health_check`, `handle_mcp_get`, `format_sse_event`, `build_sse_response`, `build_json_response`, `handle_mcp_post`, `process_request`, `format_tool_result`), which should move to `mcp/transport.rs` at ~345 lines. The inline `#[cfg(test)] mod tests` at lines 821-1043 is ~222 lines while every other test in this module already lives under `mcp/tests/`; it holds the compile-time `both_routes_take_redacting_headers` pin that reaches private handlers, so it needs a `#[path = \"server_test.rs\"]` sibling rather than a move into `mcp/tests/`. Both halves land near 300-350 lines."
    },
    "apps/desktop/src-tauri/src/menu/menu_handlers.rs": {
      "lines": 841,
      "reason": "TODO: three things share this file and only one of them is the dispatcher the module map names. Move the six macOS post-construction wrappers (`cleanup_macos_menus`, `set_macos_menu_icons`, `set_display_accelerators`, and their three `*_from_command` main-thread hops, lines 34-110) to `macos_passes.rs`: they hold no routing logic at all, just the `run_on_main_thread` hop onto `macos_appkit.rs` and `display_accelerators.rs`, and they only live here because that is where the AppKit seam used to be. Move the native-edit lane (`NativeEditSelector`, `native_edit_selector_for`, `send_native_edit_action`, lines 111-186, plus the `native_edit_selector_tests` module) to `native_edit.rs`, which gives both bars' id-to-selector table one named home next to the `objc2` call that consumes it. That leaves roughly 650 lines: `handle_menu_event` with its per-family branches and the small routing helpers each one reads (`focused_viewer_label`, `viewer_edit_action_for`, `volume_row_action`) plus their tests, which do belong together because the branch order between them is the contract."
    },
    "apps/desktop/src-tauri/src/network/manual_servers.rs": {
      "lines": 1056,
      "reason": "TODO: split it, because half the file is an inline test module. Production code ends at line 549 (address parsing, reachability probing, the `manual-servers.json` store, and discovery injection); lines 550-1072 are `#[cfg(test)] mod tests` plus `mod integration_tests`, ~522 lines together. Move both to a `manual_servers_test.rs` sibling, the convention this directory already uses in `mount_test.rs`, `share_access_test.rs`, and `sftp_host_keys_test.rs`. That leaves production at ~550 lines and tests at ~522, both well inside their budgets, and there is nothing else to decide."
    },
    "apps/desktop/src-tauri/src/operation_log/rollback.rs": {
      "lines": 860,
      "reason": "TODO: split it, because the per-item inverse acts are a unit the planner shares no locals with. `reverse_item`, `removal_target`, `missing_item`, `remove_file_if_unchanged`, `remove_dir_if_empty`, and `restore_move` (lines 528-728, ~200 lines) are each an `async fn(vm, runner, unit) -\u003e ItemResult` reached through one `match` on `InverseAction`, so they move to a `rollback/inverse.rs` beside the existing `bookkeeping.rs`, `order.rs`, `runner.rs`, and `skips.rs`. The pure decision helpers (`InverseAction`, `inverse_kind`, `inverse_action`, `is_created_in_place`, `fold_path`, `is_self_collision`, `resolve_final_state`, lines 135-313, ~180 lines) can follow into a `rollback/plan.rs`. That leaves the module doc, the refusal and report types, `check_rollbackable`, the paged `execute_rollback` loop, `summarize_inverse`, `rollback_operation`, and the reconcile at ~530 lines. This is the most data-safety-critical file in the subsystem, so it wants care, but every cut falls between functions."
    },
    "apps/desktop/src/app.css": {
      "lines": 1143,
      "reason": "About 960 of the 1,145 lines are custom-property declarations: one semantic token table in `:root` plus the light, dark, high-contrast, size-palette, date-palette, old-WebKit, and reduced-transparency blocks that each redefine the same names, so a split by token group would force every group to carry its own dark and contrast twin and make file load order load-bearing (the file itself flags stacking order where the dark and contrast blocks meet). The raw palette already lives in the imported `app-palette.css`; this is the semantic layer every component's `var()` resolves against. The ~130 lines of base element rules at the end (`.sr-only`, the `:focus-visible` outline, `@layer typography`, the per-surface leading list, `html` / `body` / `#app-root`, the scroll-bounce reset) are the one part that is not tokens and could move to a sibling, though that alone would not bring the file under the limit."
    },
    "apps/desktop/src/lib/ask-cmdr/BulkRenameReviewDialog.svelte": {
      "lines": 860,
      "reason": "TODO: split it, because it divides as 241 lines of script, 317 of markup, and 300 of `\u003cstyle\u003e`, and two pieces come out cleanly. The per-row `\u003ctr\u003e` at lines 322-539 is ~218 lines of markup carrying the allow checkbox, the thumbnail preview button, both name cells, the four warning and danger badges, the provenance badge, and the evidence and coverage cell, all keyed off one `row` plus `proposal.proposalId`; it should become a `BulkRenameReviewRow.svelte` child taking `{row, proposalId, preflighting, thumbnailUrl, focused, draftName}` and callbacks, moving roughly half the `\u003cstyle\u003e` block with it. Separately, the thumbnail token lifecycle (`thumbnailUrls`, `mintedTokens`, `thumbnailSeq`, `dropTokens`, `releaseThumbnails`, `syncThumbnails`, `withoutKeys`, the mint-and-drop `$effect`, and the `onDestroy`, ~100 script lines) is pure state with no markup dependency and belongs in a `bulk-rename-thumbnails.svelte.ts`, the pattern this directory already uses for `ask-cmdr-rename-review.svelte.ts`. The dialog itself then sits near 350 lines."
    },
    "apps/desktop/src/lib/file-explorer/pane/DualPaneExplorer.svelte": {
      "lines": 1450,
      "reason": "Its own `pane/CLAUDE.md` records the size cap and the two escape hatches, and this file has already taken both about 15 times: `createClipboardOperations`, `createFileOperationCommands`, `createPaneCommands`, `createSortOperations`, `createSwapPanes`, `createVolumeSelection`, `createEdgeFlowHandlers`, `createPaneMirror`, `createKeyDispatch`, `createMcpTabAction`, `createPaneAccessors`, `createDialogState`, `createDragDropController`, `tab-operations`, `navigate`, and `persistence-subscriber` all live outside it. What remains is ~110 lines of imports, ~390 lines of dependency wiring that closes over component-local `$derived` state and `bind:this` pane refs, and about 75 `export function` one-liners that are the component's instance API, reached by `bind:this` from the route's explorer API and the MCP surface, which cannot live anywhere but this `\u003cscript\u003e`."
    },
    "apps/desktop/src/lib/file-explorer/pane/FilePane.svelte": {
      "lines": 2200,
      "reason": "Its own `pane/CLAUDE.md` records the cap and rules out a child component here, and the file has already used both allowed hatches: about 20 `create*` factories (`createSelectionState`, `createListingLoader`, `createTypeToJumpController`, `createEnterMenu`, `createGitBrowserSync`, `createRenameFlow`, `createPaneMcpSync`, `createPaneKeyRouter`, and the rest) plus pure `.ts` helpers carry everything that can leave. What is left is 355 lines of imports, the 64 `export`s that make up `FilePaneAPI`, and the 279-line alt-view markup chain (`NetworkMountView`, `RemoteConnectView`, `MtpConnectionView`, `SearchResultsView`, `ErrorPane`, and the list views) with its 117-line `\u003cstyle\u003e`, none of which can move out of the component."
    },
    "apps/desktop/src/lib/file-explorer/pane/dialog-state.svelte.ts": {
      "lines": 800,
      "reason": "TODO: split it, because `createDialogState` returns one object literal of 34 members spanning lines 228-802, and the transfer cluster is a third of it on its own. `showTransfer`, `startTransferProgress`, `foregroundOperation`, `handleTransferConfirm`, `handleTransferCancel`, `handleTransferComplete` (~91 lines by itself), the four `handleAdopted*` members, `handleTransferQueue`, `handleTransferCancelled`, `handleTransferError`, and `handleTransferErrorClose` total ~330 lines and own their own `showTransferDialog`, `transferDialogProps`, `showTransferProgressDialog`, and `transferProgressProps` state, so they belong in a `dialog-state.transfer.svelte.ts` sub-factory spread into the returned object, which is the pattern this file already follows three times with `createTransferPaneEffects`, `createArchivePasswordFlow`, and `createAdoptedOperation`. The mkdir and mkfile pair (`showNewFolder`, `showNewFile`, `handleNewFolderCreated`, `handleNewFolderCancel`, `handleNewFileCreated`, `handleNewFileCancel`, ~75 lines) is a smaller second one. That leaves the alert and delete members plus the cross-cutting `anyDialogOpen`, `closeConfirmationDialog`, `handleDialogRenderFailure`, `dismissAllAfterRenderFailure`, `isConfirmationDialogOpen`, and `confirmOpenDialog` at ~350 lines, and the test siblings are already split by exactly these topics."
    },
    "apps/desktop/src/lib/file-explorer/pane/listing-loader.ts": {
      "lines": 805,
      "reason": "TODO: the `onListingError` handler inside `loadDirectory` (the MTP fallback, the archive-password prompt, the not-connected refusal, and the deleted-path existence probe plus walk-up, about 110 lines) is one decision, what a failed listing turns into, and reads only the load's captured `volumeId` / `loadPath` plus a handful of deps. Move it to `listing-error-outcome.ts` as a function taking those, beside `listing-loader.outcomes.test.ts`, which already tests exactly that branch."
    },
    "apps/desktop/src/lib/file-explorer/views/BriefList.svelte": {
      "lines": 1111,
      "reason": "TODO: Brief is the only view that still inlines what Full has already factored out. Its roughly 830-line script holds the prefetch buffer (`cachedEntries`, `cachedRange`, `isFetching`, `parentDirStats`, `fetchVisibleRange`, `visibleColumns`, and the hard-reset/soft-refresh `$effect`, about 220 lines) and the whole `handleMouseDown` drag-payload decision plus `handleClick`, `handleDoubleClick`, and `maybeStartClickToRename` (about 120 lines), while `FullList` reaches the same behavior through `full-list-cache.svelte.ts` and `full-list-mouse.ts`. Mirror those two: a `brief-list-cache.svelte.ts` wrapping the `file-list-utils.ts` helpers it already calls, and a `brief-list-mouse.ts` with a `planRowMouseDown` twin, then move `buildDirTooltip` and `buildNameTooltip` (about 40 lines) beside the `full-list-utils.ts` building blocks they compose. Markup (about 150) and `\u003cstyle\u003e` (about 170) are already lean and stay put."
    },
    "apps/desktop/src/lib/file-explorer/views/FullList.svelte": {
      "lines": 1215,
      "reason": "TODO: The script (about 635 lines) is already down to wiring since `full-list-cache.svelte.ts`, `full-list-git-column.svelte.ts`, `full-list-mouse.ts`, and `measure-column-widths.ts` left, and about 130 of it is the `Props` interface, which can't move. The remaining bulk is the Size cell: one `col-size` span spanning about 90 markup lines over five nested branches (`sizeOverride`, the `dir` and `scanning` placeholders, the lower-bound prefix, the size triads, the hourglass, the mismatch warning) plus about 60 lines of `.col-size` and `.size-*` style. Pull it into a `FullListSizeCell.svelte` taking `file`, `sizeOverride`, `dirDisplaySize`, `fileDisplaySize`, `sizeFormatOpts`, `isSizeUpdating`, and `showSizeMismatchWarning`; it keeps importing `getDirSizeDisplayState` and `pickSizeDisplay` from `full-list-utils.ts`, so the renderer-and-measurer single-source contract that `views/CLAUDE.md` pins stays intact. That is roughly 150 lines out and leaves the row template readable."
    },
    "apps/desktop/src/lib/file-operations/transfer/TransferDialog.svelte": {
      "lines": 976,
      "reason": "TODO: Script 562 / markup 247 / style 228, with the script already orchestration only (`transfer-scan-state.svelte.ts`, `transfer-conflict-check.svelte.ts`, `transfer-dest-exists.svelte.ts`, and `transfer-dialog-logic.ts` hold the logic). Two presentational blocks can follow the `ScanPhaseBody.svelte` and `CompressEstimateLine.svelte` precedent in the same directory: a `TransferScanTallies.svelte` for the `.scan-stats` row plus the `scan.scanFailure` retry notice (about 45 markup and 50 style), and a `TransferConflictsSection.svelte` for the `isCheckingConflicts` to `conflictCheckUnknown` to merge-info, summary, `RadioGroup`, and type-mismatch chain (about 70 markup and 55 style). That is roughly 220 lines out, leaving `handleConfirm` and `handleCancel`, whose dense rationale comments are the part of this file worth protecting."
    },
    "apps/desktop/src/lib/file-operations/transfer/transfer-progress-state.svelte.ts": {
      "lines": 803,
      "reason": "One closure over shared mutable teardown flags (`operationId`, `backgrounded`, `destroyed`, `closed`, `cancelRequestedBeforeId`, and the two wind-down timers) that birth, the four outcome effects, and the command handlers all read and write, plus about 90 lines of pass-through getters over `bound.current` that are the dialog's read surface; splitting `beginOperation` out would mean threading eight callbacks back in. The plain-`let` staleness hazard and the settle contract that hold the halves together are argued in the module header and in `transfer/CLAUDE.md`."
    },
    "apps/desktop/src/lib/onboarding/StepAi.svelte": {
      "lines": 826,
      "reason": "TODO: Script 376 / markup 210 / style 266, and the two biggest markup blocks are static presentation carrying most of that style. The with-AI-versus-without comparison table is about 60 markup lines plus the `.comparison*`, `.with-ai-head`, and `.row-head*` rules (about 45 style) and the `code` and `chip` snippets it owns; the three FDA-outcome banners (`granted`, `denied`, `stuck`) are about 35 markup lines plus the `.banner*` rules (about 50 style) and are one shape differing only by tone, icon, title key, and body. Extract an `AiComparisonTable.svelte` and a `StepAiBanner.svelte` taking `tone`, `icon`, `titleKey`, and a body snippet, matching how `CloudProviderPicker`, `CloudProviderSetup`, and `FdaBadge` already sit beside this file. That is roughly 190 lines out, leaving the choice persistence, the confirm-once missing-key gate, and the local-download lifecycle, which are what the step is about."
    },
    "apps/desktop/src/lib/query-ui/QueryDialog.svelte": {
      "lines": 823,
      "reason": "The wiring seam itself: script 524 / markup 229 / style 81, where the script is event handlers plus the documented lifecycle contracts and the markup is prop threading into `QueryBar`, `ModeChips`, `FilterChips` (about 32 props), `QueryResults` (about 30), and `RecentItemsPopover`. The logic already lives in the four tested siblings `query-runner.svelte.ts`, `recent-popover.svelte.ts`, `query-shortcuts.ts`, and `result-actions.ts`, the split `query-ui/CLAUDE.md` § Module map states; what remains is the one place those pieces meet, plus the three state-ownership contracts spelled out in the file header."
    },
    "apps/desktop/src/lib/query-ui/QueryResults.svelte": {
      "lines": 945,
      "reason": "TODO: Three separable concerns share one file (script 434 / markup 214 / style 323). The status bar is the first: `getStatusText`, `walkProgress`, `waitElapsed`, the `waitNow` ticker, and the `createAnnouncementThrottle` live region (about 80 script) plus its markup (about 40) and `.status-*` style (about 55) already take every helper from `query-stream.ts`, so a `QueryStatusBar.svelte` taking the counts and the `LiveRunView` lifts roughly 175 lines cleanly. The second is the Name-track measurement: `nameTrack`, `measureName`, `ensureMeasure`, `readFont`, the viewport `ResizeObserver`, and the re-measure `$effect` (about 125 script) belong in a `name-column-measure.svelte.ts` factory beside the pure `name-column-width.ts` it already calls, returning `gridTemplate` and `animateNameTrack`; pass `gridTemplate` down as one string so the one-template-feeds-header-and-rows rule in `query-ui/CLAUDE.md` still holds. That leaves the state-branch chain and the row template at roughly 670 lines."
    },
    "apps/desktop/src/lib/tauri-commands/index.ts": {
      "lines": 915,
      "reason": "A pure barrel: 944 lines of `export { … } from './x'` and `export type { … }` across about 40 domain sub-files, one line per re-exported symbol, with zero declarations, imports, or logic anywhere in it. `tauri-commands/CLAUDE.md` makes it the single stable import path precisely so sub-file layout can shift behind it, so its length tracks the IPC surface and cutting it would mean either fewer commands or breaking that contract."
    },
    "apps/desktop/src/lib/ui/ModalDialog.svelte": {
      "lines": 823,
      "reason": "TODO: Script 473 / markup 70 / style 305, and about 120 script lines are the `Props` interface whose per-prop docs are this primitive's API contract and can't leave. The separable piece is the edge-and-corner resize: `ALL_RESIZE_DIRECTIONS`, `cssLength`, `clamp`, `minimumPanelHeight`, and the geometry inside `handleResizePointerDown` (the growth compensation that keeps the opposite edge fixed, and the `verticalDrift` centered-versus-pinned rule) are about 110 lines of pure arithmetic currently reachable only by dispatching jsdom pointer events at `.resize-band`. Move them to a `modal-resize.ts` exporting a pure `resizeStep({ direction, startRect, startPosition, delta, limits, verticalDrift })` that returns the next width, height, and position, leaving `handleResizePointerDown` as listener plumbing; the drift rule then gets direct unit tests rather than the DOM-driven ones in `ModalDialog.svelte.test.ts`. The `\u003cstyle\u003e` block stays as is: it is one primitive's chrome, and `ui/CLAUDE.md` pins several of its rules as invariants."
    },
    "apps/desktop/src/routes/(main)/+page.svelte": {
      "lines": 831,
      "reason": "TODO: Script 661 / markup 134 / style 90. The route's `CLAUDE.md` is right that `$state` belongs here and logic leaves through setter-and-getter contexts, and most of it already has (`startup-gates.ts`, `window-services.ts`, `command-dispatch.ts`, `listener-setup.ts`, `global-keydown.ts`, `mouse-nav.ts`). Licensing is the holdout: `showExpiredModal`, `expiredOrgName`, `expiredAt`, `showCommercialReminder`, `showAboutWindow`, `showLicenseKeyDialog`, and `licenseIsPersonalUse`, their five `handle*Close` and `handleLicenseKeySuccess` handlers, and the roughly 30-line `loadLicenseStatus` block in `onMount` come to about 110 lines nothing else on the page reads. Move them into a `licensing-dialogs.svelte.ts` trigger module beside the `licensing-store.svelte.ts` they already call, exactly how `whats-new-trigger.svelte.ts`, `acknowledgements-trigger.svelte.ts`, `operation-log-trigger.svelte.ts`, and `suggested-ops-trigger.svelte.ts` already serve four of the dialogs in this same markup; the page then renders off `licensingDialogsState.*` and `windowTitle` reads one getter."
    },
    "apps/desktop/src/routes/debug/DebugSmbDiagnosticsPanel.svelte": {
      "lines": 1040,
      "reason": "TODO: 650 of its lines are markup, and roughly 50 near-identical label/tooltip/value triples make up most of that: every metric row spells out a `\u003cspan class=\"smb-label\"\u003e` with an inline `use:tooltip` info glyph plus a `\u003cspan class=\"smb-value\"\u003e`, which the formatter wraps to 8-10 lines each. Extract an `SmbMetric.svelte` row (props: label, tip, value, optional value tip) and an `SmbCard.svelte` title-plus-grid wrapper beside this file; each of the 13 cards (Flow, Deadlines and keepalive, Session revivals, Wire traffic, Requests, Responses, Protocol events, Errors, Session, Negotiated, Client, DFS cache, DFS extra connections) then reads as a title plus one line per metric, cutting the markup to roughly 250 lines and moving the `.smb-card*` / `.smb-label` / `.smb-value` / `.info-icon` rules out of the 240-line `\u003cstyle\u003e` into the two primitives. The 150-line `\u003cscript\u003e` (polling, the `fmt*` helpers, the volume select) stays."
    },
    "apps/desktop/src/routes/viewer/+page.svelte": {
      "lines": 1590,
      "reason": "TODO: The search bar is the one large presentational block this directory has not extracted yet: roughly 112 lines of markup (the `TextInput`, the `Aa` and `.*` toggles, the five-branch match counter, the stop/prev/next/close buttons, the progress bar), bound entirely to the `search` composable, plus roughly 70 lines of `.search-bar` / `.search-toggle` / `.match-count` / `.search-error` / `.search-spinner` / `.search-progress*` CSS. Move it to `ViewerSearchBar.svelte` beside `ViewerToolbar.svelte` and `ViewerStatusBar.svelte`, and move the binary-warning banner (roughly 30 markup lines plus its `.binary-warning*` rules) to `ViewerBinaryWarning.svelte`. That lands near 1,290 lines and no lower: the remaining 885-line `\u003cscript\u003e` is pinned here by the contract in `CLAUDE.md` (the `$state` lives on the page, the roughly 20 `createViewer*` composables take getters, and the `$effect`s delegate to `run*Effect()`), and `openViewerSession` writes a dozen of those `$state` vars directly, so lifting it out would mean threading a dozen setters through."
    },
    "apps/website/src/dev/blog-editor/entry.ts": {
      "lines": 1096,
      "reason": "TODO: Two independent things share this file. The live preview renderer is roughly 320 lines (`renderPreview`, `expandInlineIcons`, `iconSvgHtml`, `rewritePreviewImageSources`, `expandBlogMedia`, `figureCell`, `buildSlider`, `lightboxFigure`, `activateCompareSliders`, `SLANT_MAX`, `isMeaningfulNode`, `isImageCell`, `captionFor`) and it mirrors `src/plugins/blog-media.ts` and `BlogCompareSlider.astro` rather than the editor; everything below `renderPreview` takes a container and transforms it, with no reach into the module-level `entry` state. Move it to a `preview.ts` sibling exporting `renderPostBody(container, html)` and `activateCompareSliders`, so the keep-in-sync-with-the-plugin obligation lives in one named file. Then move the textarea editing helpers (`attachMarkdownShortcuts`, `wrapSelection`, `insertLink`, `insertAtCursor`, roughly 70 lines) to `markdown-shortcuts.ts`, and the tail of pure helpers (`stableHash`, `slugify`, `todayString`, `formatTime`, `formatLongDate`, `arrayBufferToBase64`, `altTextFromFilename`, `assetUrlFor`, `fetchJson`, `element`, `errorMessage`, roughly 120 lines) to `utils.ts`. `entry.ts` keeps the DOM handles, `attachListeners`, and the load/save/publish/backup flow, at roughly 600 lines."
    },
    "crates/cmdr-archive/src/volume.rs": {
      "lines": 891,
      "reason": "TODO: The stream adapters are self-contained and touch none of `ArchiveVolume`'s fields: `ArchiveVolumeReadStream`, `ArchiveSequentialExtract` with its `impl SequentialExtract`, and `MemberStream` (lines 667-804), plus `VolumeByteSource` and its `impl ArchiveByteSource` (lines 830-888), depend only on the reading core's types and `to_volume_error`. Move those roughly 200 lines to a `volume_streams.rs` sibling, leaving `parse_blocking` behind since `index` and `load` call it. `volume.rs` then sits near 770 lines holding what `CLAUDE.md` says it holds: `ArchiveVolume`, its inherent impl (`index`, `load`, `open_stream`, `open_remote_source`, the password store, the content watch), the single `impl Volume` block, and the two type mappers `node_to_entry` and `to_volume_error`."
    },
    "crates/cmdr-fs/src/volume/mod.rs": {
      "lines": 1680,
      "reason": "Over half its 1,680 lines are doc comments, because this is the `Volume` trait definition, the vocabulary every storage backend in Cmdr speaks, plus the two small stream traits its methods hand back (`VolumeReadStream`, `SequentialExtract`). Rust needs a trait in one block, and the prose is the deliverable: contracts like `delete` refusing to recurse and `rename(force: false)` refusing an existing destination are stated here and enforced by the shared assertions in `conformance.rs`. Everything that could leave already has, as the module declarations at the foot of the file show: `types`, `ids`, `capabilities`, `in_memory`, `mkdir_all`, `patching`, `scan_walk`, `host/`, and a dozen more."
    },
    "crates/cmdr-index/src/indexing/aggregator/mod.rs": {
      "lines": 810,
      "reason": "One algorithm with five entry points: `topological_sort_bottom_up` plus `compute_bottom_up` (the 0-absorbing `min_subtree_epoch` rollup) is the core, and `compute_all_aggregates_reported`, `compute_all_aggregates_with_maps`, `compute_partial_aggregates`, `compute_partial_aggregates_sql`, `compute_subtree_aggregates`, and `backfill_missing_dir_stats` differ only in where their input maps come from and which subset of rows they write back. The SQL readers already live in `readers.rs` behind a one-directional compute-to-readers seam and the tests in `tests.rs`, and 248 lines are doc comments recording why all four callers must supply the separate `listed_epoch` map (the `CLAUDE.md` must-know that a missing one re-breaks coverage). It sits 47 lines over budget, and splitting by entry point would strand the shared core in a third module."
    },
    "crates/cmdr-index/src/indexing/lifecycle/phased_bench.rs": {
      "lines": 1415,
      "reason": "A throwaway measurement harness behind `#[cfg(all(test, target_os = \"macos\"))]`, whose eight `#[ignore]`d arms exist to attribute one wall-clock difference to a cause, and every one of them runs over the same `Bench` fixture (`stitch_dir`, `run_phase`, `cover_frontier_root`, `frontier_under`, `drain`), its three instruments (`Sampler`, `Pump`, `Browser`), and the one `Report::print` whose output `docs/notes/phased-vs-bulk-index-2026-08-14.md` quotes. Splitting the arms off would leave each of them importing a sibling's private struct, and the inline `mod tests` that guards the harness against reporting a fast wrong number is only 168 lines, so moving it to a sibling would still leave the file over the test budget."
    },
    "crates/cmdr-index/src/indexing/lifecycle/phases/mod.rs": {
      "lines": 866,
      "reason": "`Machine` is one state machine over the shared fields of a run (`work`, `writer`, `events`, `visits`, `progress`, `coverage_phase`, `freshness`), and its 20 methods are the steps of `run()`: `initial_queue`, `run_phase`, `walk_all`, `walk_group`, `take_a_visit`, `frontier_under`, `order`, `drain`, `take_stock`, `finish`. The area is already split around it into `queue.rs`, `stitch.rs`, `grouping.rs`, `visits.rs`, and `completion.rs`, which `phases/CLAUDE.md` records, so what remains is the driver plus `PhaseHandle` and `start`. 331 of the 864 lines are doc comments carrying the measurements behind the per-phase drain and the group cadence."
    },
    "crates/cmdr-index/src/indexing/scanner/mod.rs": {
      "lines": 935,
      "reason": "TODO: Two clusters would leave without argument. First, `ScanRoot` (with `is_volume` and `stays_on_one_device`), `WalkPolicy` (with `for_walk`, `with_device_probe`, `excludes`, `leaves_the_volume`), and the two `cfg`-gated `device_of` bodies run from line 125 to 264, roughly 140 lines, and the sibling that tests them is already called `policy_tests.rs` with no `policy.rs` to pair with. Second, the value and error types run from line 266 to 560, roughly 295 lines: `CoveredEntry`, `ScanConfig` with its `Default`, `ScanProgress` and `ScanProgressSnapshot`, `ScanHandle`, `ScanSummary`, `ScanOutcome` with `into_result`, and `ScanError` with its `Display`, `std::error::Error`, and `From\u003cstd::io::Error\u003e` impls; those belong in a `types.rs`. That leaves `mod.rs` at roughly 600 lines holding what its name promises: the module wiring and re-exports, `send_unreadable_marks`, the three entry points `scan_volume` / `scan_subtree` / `cover_subtree`, their shared `walk_subtree` body, and the 220-line `run_scan` whose post-walk steps share locals."
    },
    "crates/cmdr-index/src/indexing/store/entries.rs": {
      "lines": 803,
      "reason": "It sits three lines over the 800-line budget, and it is one `impl IndexStore` block of SQL accessors over the one `entries` table: the child-listing and `for_each_*` readers, the id / inode / component lookups, the insert family, `update_entry` / `rename_entry` / `move_entry`, and the post-order subtree delete (`delete_descendants_inner`, `delete_batched`, `child_ids_of`, `delete_rows_by_id`) that the store `CLAUDE.md` pins as a data-safety invariant. The store is already split per concern into `paths.rs`, `dir_tree.rs`, `dir_stats.rs`, `meta.rs`, `schema.rs`, `connection.rs`, and `collation.rs`, with the tests under `tests/`, and 246 of the lines are doc comments, including the `ERR-AYVM4` rationale behind `inode_to_sql` and `size_to_sql`."
    },
    "crates/cmdr-index/src/indexing/tests/integration_tests.rs": {
      "lines": 1227,
      "reason": "TODO: split by fixture family; the three groups share nothing but the file, and each serializes on a different mutex. Move the `ReadPool` group (`setup_db_for_pool`, `enrichment_under_contention`, `enrichment_under_contention_non_root`, and the five `read_pool_*` tests, all on `READ_POOL_TEST_MUTEX`) to a sibling `read_pool_tests.rs` at roughly 300 lines, and the registry group (`reset_indexing_for_test`, `root_is_registered`, `reserve_initializing_for`, `install_initializing_phase`, the three `should_auto_start_indexing_*`, `try_reserve_initializing_succeeds_only_from_disabled`, the two `stop_indexing_*`, `clear_index_from_initializing_removes_instance_and_deletes_db`, and the two `shutdown_*`, all on the `INDEX_REGISTRY` guard mutex) to `lifecycle_phase_tests.rs` at roughly 335 lines; what stays is the enrichment end-to-end path with `open_temp_store` and `make_file_entry`, roughly 635 lines. `tests/mod.rs` already declares six sibling test modules, so this is the directory's own shape."
    },
    "crates/cmdr-index/src/indexing/tests/stress_tests_concurrency.rs": {
      "lines": 1301,
      "reason": "TODO: move the hand-run stall repro out. `test_listings_complete_under_reconciler_load_and_rapid_navigation` plus the 48-line iteration-log comment above it is roughly 390 lines, is `#[ignore]`d, and asserts a listing-latency SLA rather than a concurrency invariant, so it belongs in a sibling `stress_tests_listing_sla.rs` (it builds its own `EntryRow` values, so it takes no helper with it). The five real scenarios (`concurrent_scan_with_buffered_events_and_replay`, `concurrent_batch_inserts_with_aggregation`, `concurrent_scan_with_enrichment_reads`, `live_event_storm_with_concurrent_reads`, `mixed_storm_reaches_consistent_fixed_point`) each drive actor threads against one writer with locals they can't share, and stay at roughly 900 lines, under the test budget."
    },
    "crates/cmdr-index/src/indexing/writer/mod.rs": {
      "lines": 1625,
      "reason": "TODO: split; the area already runs 12 one-job siblings and `mod.rs` carries four things that aren't the loop. Move `WriteMessage` + `AggSource` + `WRITER_CHANNEL_CAPACITY` (roughly 280 lines, mostly per-variant rationale) to `message.rs`, `MutationTracker` + `WRITER_GENERATION` (roughly 130) to `mutation_tracker.rs`, the `IndexWriter` handle with `spawn` / `spawn_for` / the test-observable accessors / `flush_blocking` / `shutdown` (roughly 405) to `handle.rs`, and `WriterStats` + `StatsSnapshot` + `AccumulatorMaps` (roughly 195) to `stats.rs`. That leaves `writer_loop` and `process_message`, the one state machine whose ambient locals (`batch`, `repairs`, `rollups`, `probe`, `heal_latch`, `propagate_deltas`) are genuinely shared, at roughly 630 lines."
    },
    "crates/cmdr-index/src/media_index/scheduler/enrich_tests.rs": {
      "lines": 1262,
      "reason": "TODO: split along the section banners the file already draws, into the per-concern `*_tests.rs` siblings this directory keeps (`kick_tests.rs`, `live_tests.rs`, `reclaim_tests.rs`, `coalescing_tests.rs`). The CLIP two-part-staleness group (`clip_state`, `clip_pass`, `model_absent_is_vision_only_no_clip_embedding`, `installing_clip_embeds_without_re_running_vision`, `a_clip_model_bump_re_embeds_without_touching_vision`, `delete_clip_model_removes_the_model_and_every_volumes_embeddings`) is roughly 210 lines over a different pass, and the live scoped-walk group (`seed_media`, `touched`, the two `walk_in_dirs_*`, `scoped_gc_spares_rows_in_untouched_dirs`, `whole_store_gc_over_a_scoped_walk_would_wipe_untouched_dirs`, `a_scoped_tick_promotes_a_lone_raw_and_gcs_the_deleted_jpg`) another roughly 205. Move them to `enrich_clip_tests.rs` and `enrich_scoped_tests.rs`, importing `build_index` / `media_writer` / `no_op_hooks` from here the way `live_tests.rs` already imports from `kick_tests.rs`, leaving roughly 850 lines of core-pass, GC, throttle, and progress tests."
    },
    "crates/cmdr-mtp/src/connection/directory_ops.rs": {
      "lines": 983,
      "reason": "TODO: split; two blocks aren't directory ops at all. `handle_device_disconnected` plus the `disconnect_test_hooks` tally (roughly 90 lines) is session teardown and belongs beside its documented counterpart in `session_reset.rs`, and `clear_all_listing_caches` / `clear_listing_caches_for_device` / `invalidate_listing_cache` (roughly 55 lines) belong in `cache.rs`, which already owns `ListingCache`. Then move the background scan path (`list_directory_for_scan` and `list_directory_inner_with_progress`, roughly 270 lines of yield-point and metadata-batch work that foreground listings deliberately avoid) to a sibling `scan_listing.rs`, leaving the foreground path with `fresh_cached_listing`, `finalize_listing`, and `entry_for_object` at roughly 530 lines."
    },
    "crates/cmdr-mtp/src/connection/mod.rs": {
      "lines": 1204,
      "reason": "TODO: split; the file even carries a comment listing which impl blocks live in submodules, and two more belong there. Move `connect` with `resolve_device_location_id`, `open_device`, `probe_write_capability`, and `get_storages` (roughly 400 lines, one device-open conversation end to end) to `connect.rs`, and the storage-level handlers `handle_storage_added`, `handle_storage_removed`, `invalidate_storage_cache`, and `get_live_storage_space` (roughly 230) to `storage_ops.rs`. That leaves the vocabulary (`MtpDisconnectReason`, `DeviceWatch`, `MtpObjectInfo`, `ConnectedDeviceInfo`, `DeviceEntry`), the `MtpConnectionManager` struct, the priority-gate accessors, `disconnect`, and the small path and icon helpers at roughly 500 lines."
    },
    "crates/cmdr-sftp/src/transport.rs": {
      "lines": 945,
      "reason": "The crate's headline invariant is that `russh` is named in exactly one module so a breaking minor stays one file's problem (eight breaking minors in eight months), stated in the module doc and again in `crates/cmdr-sftp/CLAUDE.md`; everything russh-free has already been pushed out to `auth.rs`, `trust.rs`, `known_hosts.rs`, `extensions.rs`, and `errors.rs`, so what remains is one dial conversation (`open_session` to `authenticate` to `hello`) plus the `PendingEngine` teardown guard and the two `Handler` impls, under a 57-line module doc for the three abort hazards the shape defends against."
    },
    "scripts/check/checks/common.go": {
      "lines": 803,
      "reason": "TODO: split; `DETAILS.md` § Key files scopes this file to the core types and command plumbing every check builds on, and two unrelated toolchains have grown inside it. Move the pinned-tool installer (`ResolveDevSecret`, `InstallPinnedBinary`, `extractFromTarGz`, `installExecutable`, `NextestVersion`, `EnsureCargoNextest`, `GoToolsBinPath`, `goToolBuildInfo`, `EnsureGoTool`, `goToolIsCurrent`, `parseGoToolBuildInfo`, `pinnedModuleVersion`, roughly 265 lines) to `tool-install.go`, and the frontend lane runners (`runOxfmtCheck`, `parseOxfmtFileCount`, `runESLintCheck`, `runStylelintCheck`, `runKnipCheck`, `runImportCyclesCheck`, `buildFindArgs`, roughly 195 lines) to `frontend-lanes.go`. Both stay in the package, so `runner-sources.go` still reaches them by following `Run`, and what is left is the vocabulary (`App`, `CheckResult`, `CheckContext`, `CheckDefinition`), the process tracker, and the output helpers at roughly 420 lines."
    },
    "scripts/check/checks/desktop-rust-cfg-gate_test.go": {
      "lines": 1228,
      "reason": "TODO: split the end-to-end group out. The `TestRunCfgGate_*` tests write whole temp workspaces through `seedCfgGateWorkspaceRoot` and `writeCfgGateWorkspace` and run to roughly 460 lines across the two banners `RunCfgGate integration tests` and `Workspace coverage`; everything else is string-level unit tests over `extractMacOSCrateModules`, `findCfgGatedModules`, `hasMacOSCfgAttribute`, `isMacOSGateAttribute`, `collectAttribute`, `isAttributeContinuation`, `buildModuleGatedFileSet`, and `scanForUngatedUses`, each carrying its fixture inline. Move the first group plus its two seed helpers to `desktop-rust-cfg-gate_run_test.go`, leaving roughly 770 lines here, and name the second file in `DETAILS.md` § Key files, whose layout rule currently reads as one test file per check."
    },
    "scripts/check/checks/lock-poison.go": {
      "lines": 890,
      "reason": "TODO: split off the Rust lexer, which carries no lock-poison vocabulary at all. `findBraceBlock`, `nextMeaningful`, `blankLineComment`, `lineCommentStart`, `skipLiteral`, `opensCharLiteral`, `matchArm`, `lockMatchErrArm`, `splitMatchArms`, and the `matchArmSplitter` state machine are roughly 205 lines of generic brace, comment, literal, and match-arm scanning, and `DETAILS.md` already records another scanner modeled on this one and reusing its `#[cfg(test)]`-mod skip, so a sibling `rust-lex.go` gives that reuse a home. Moving the report builders (`appendNonEmpty`, `formatLockPoisonViolations`, `formatLockSwallows`, `formatLockPoisonStaleness`, roughly 90 lines) to `lock-poison-report.go` leaves the two lanes, their patterns, the classifier, and the allowlist at roughly 595 lines."
    }
  }
}
