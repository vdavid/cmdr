# cargo-audit configuration
# See https://github.com/rustsec/cargo-audit for documentation

[advisories]
# Ignored advisories. Each one needs a rationale; the rationale lives in exactly one
# place, so this file points rather than restates.
ignore = [
    # The Marvin timing attack in `rsa`, which reaches us through `russh` -> `ssh-key`.
    # What's exposed, why we keep RSA compiled, what a fix would take, and when to
    # re-check: the matching entry in `deny.toml`'s `[advisories] ignore`.
    "RUSTSEC-2023-0071",
]
