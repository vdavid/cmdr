{
  "tools": [
    {
      "description": "Switch a pane to a volume by name (as listed in cmdr://state volumes): a disk, SMB share, MTP device, or Network. To move within the current volume, use nav_to_path instead.",
      "inputSchema": {
        "properties": {
          "name": {
            "description": "Volume name to select",
            "type": "string"
          },
          "pane": {
            "description": "Which pane to switch",
            "enum": [
              "left",
              "right"
            ],
            "type": "string"
          }
        },
        "required": [
          "pane",
          "name"
        ],
        "type": "object"
      },
      "name": "select_volume"
    },
    {
      "description": "Navigate a pane to a path: absolute, ~-relative, or virtual (mtp://, smb://). Prefer this over stepping with nav_to_parent when you know the target. Archive paths are transparent, so a path through foo.zip/inner navigates inside the archive.",
      "inputSchema": {
        "properties": {
          "pane": {
            "description": "Which pane to navigate",
            "enum": [
              "left",
              "right"
            ],
            "type": "string"
          },
          "path": {
            "description": "Path to navigate to: absolute, ~-relative, or virtual (mtp://, smb://)",
            "type": "string"
          }
        },
        "required": [
          "pane",
          "path"
        ],
        "type": "object"
      },
      "name": "nav_to_path"
    },
    {
      "description": "Navigate the focused pane up to its parent folder.",
      "inputSchema": {
        "properties": {},
        "required": [],
        "type": "object"
      },
      "name": "nav_to_parent"
    },
    {
      "description": "Go back to the focused pane's previous folder in its navigation history.",
      "inputSchema": {
        "properties": {},
        "required": [],
        "type": "object"
      },
      "name": "nav_back"
    },
    {
      "description": "Go forward again (undo a nav_back) in the focused pane's navigation history.",
      "inputSchema": {
        "properties": {},
        "required": [],
        "type": "object"
      },
      "name": "nav_forward"
    },
    {
      "description": "Load the file window around an index in a large (paginated) directory so those rows appear in cmdr://state. Needed before move_cursor / select can reach a row outside the currently loaded range.",
      "inputSchema": {
        "properties": {
          "index": {
            "description": "Zero-based index to scroll to",
            "type": "integer"
          },
          "pane": {
            "description": "Which pane to scroll",
            "enum": [
              "left",
              "right"
            ],
            "type": "string"
          }
        },
        "required": [
          "pane",
          "index"
        ],
        "type": "object"
      },
      "name": "scroll_to"
    },
    {
      "description": "Focus a pane and move its cursor to a row, by zero-based index or by filename (give one). Flushes pane state, so a following copy / move / delete / rename acts on this row. A missing filename or out-of-range index is an honest error, never a silent no-op.",
      "inputSchema": {
        "properties": {
          "filename": {
            "description": "Filename to move cursor to",
            "type": "string"
          },
          "index": {
            "description": "Zero-based index to move cursor to",
            "type": "integer"
          },
          "pane": {
            "description": "Which pane to move cursor in",
            "enum": [
              "left",
              "right"
            ],
            "type": "string"
          }
        },
        "required": [
          "pane"
        ],
        "type": "object"
      },
      "name": "move_cursor"
    },
    {
      "description": "Open the item under the cursor, like pressing Enter: enter a folder, open a file, or connect a network host / share.",
      "inputSchema": {
        "properties": {},
        "required": [],
        "type": "object"
      },
      "name": "open_under_cursor"
    },
    {
      "description": "Select files in a pane by names, by an index range (start + count), or all; count=0 clears. Focuses the pane and flushes state, so a following copy / move / delete / compress acts on this selection. names errors if any name isn't in the listing.",
      "inputSchema": {
        "properties": {
          "all": {
            "description": "Select all files",
            "type": "boolean"
          },
          "count": {
            "description": "Number of items from start. 0 clears selection",
            "type": "integer"
          },
          "mode": {
            "description": "Selection mode (default: replace)",
            "enum": [
              "replace",
              "add",
              "subtract"
            ],
            "type": "string"
          },
          "names": {
            "description": "Filenames to select. Errors if any name isn't in the listing.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "pane": {
            "description": "Which pane to select in",
            "enum": [
              "left",
              "right"
            ],
            "type": "string"
          },
          "start": {
            "description": "Zero-based start index",
            "type": "integer"
          }
        },
        "required": [
          "pane"
        ],
        "type": "object"
      },
      "name": "select"
    },
    {
      "description": "Copy the selection (else the cursor item) to the other pane. Without autoConfirm, opens the confirm dialog. With autoConfirm, starts at once and returns the operationId (await operation_complete, or steer with queue). onConflict resolves file clashes.",
      "inputSchema": {
        "properties": {
          "autoConfirm": {
            "description": "When true, dialog opens and immediately confirms without waiting for user interaction. Returns once the operation starts.",
            "type": "boolean"
          },
          "onConflict": {
            "description": "Conflict resolution policy for clashing FILES (only when autoConfirm is true). Folders always merge: a source folder landing on a same-named dest folder merges into it, and this policy governs the files inside. Default: skip_all",
            "enum": [
              "skip_all",
              "overwrite_all",
              "rename_all"
            ],
            "type": "string"
          }
        },
        "required": [],
        "type": "object"
      },
      "name": "copy"
    },
    {
      "description": "Move the selection (else the cursor item) to the other pane. Without autoConfirm, opens the confirm dialog. With autoConfirm, starts at once and returns the operationId (await operation_complete, or steer with queue). onConflict resolves file clashes.",
      "inputSchema": {
        "properties": {
          "autoConfirm": {
            "description": "When true, dialog opens and immediately confirms without waiting for user interaction. Returns once the operation starts.",
            "type": "boolean"
          },
          "onConflict": {
            "description": "Conflict resolution policy for clashing FILES (only when autoConfirm is true). Folders always merge: a source folder landing on a same-named dest folder merges into it, and this policy governs the files inside. Default: skip_all",
            "enum": [
              "skip_all",
              "overwrite_all",
              "rename_all"
            ],
            "type": "string"
          }
        },
        "required": [],
        "type": "object"
      },
      "name": "move"
    },
    {
      "description": "Zip the selection into a new archive in the other pane. Without autoConfirm, opens the confirm dialog. With autoConfirm, starts and returns the operationId — unless the target archive exists, where the dialog stays open to confirm the overwrite.",
      "inputSchema": {
        "properties": {
          "autoConfirm": {
            "description": "When true, the dialog opens and immediately confirms without waiting for user interaction, returning once the compress starts. Exception: if the target archive already exists, the dialog stays open for the user to confirm the overwrite rather than replacing it silently.",
            "type": "boolean"
          }
        },
        "required": [],
        "type": "object"
      },
      "name": "compress"
    },
    {
      "description": "Delete the selection (else the cursor item). Without autoConfirm, opens the confirm dialog. With autoConfirm, starts at once and returns the operationId (await operation_complete on it). mode presets trash vs permanent; omit for the pane's default.",
      "inputSchema": {
        "properties": {
          "autoConfirm": {
            "description": "When true, dialog opens and immediately confirms without waiting for user interaction. Returns once the operation starts.",
            "type": "boolean"
          },
          "mode": {
            "description": "trash = move to Trash; delete = permanent. Omit for the pane's default (a volume without a trash forces permanent).",
            "enum": [
              "trash",
              "delete"
            ],
            "type": "string"
          }
        },
        "required": [],
        "type": "object"
      },
      "name": "delete"
    },
    {
      "description": "Rename an item (the named item, else the cursor item) in a pane. Without autoConfirm, opens the inline rename editor prefilled with newName for the user to confirm. With autoConfirm, renames directly (errors if the name already exists).",
      "inputSchema": {
        "properties": {
          "autoConfirm": {
            "description": "When true, renames directly without the review editor. Returns once the rename lands.",
            "type": "boolean"
          },
          "name": {
            "description": "Current filename to rename. Defaults to the cursor item. Errors if it isn't in the listing.",
            "type": "string"
          },
          "newName": {
            "description": "The proposed new name (a name, not a path).",
            "type": "string"
          },
          "pane": {
            "description": "Which pane to rename in. Defaults to the focused pane.",
            "enum": [
              "left",
              "right"
            ],
            "type": "string"
          }
        },
        "required": [
          "newName"
        ],
        "type": "object"
      },
      "name": "rename"
    },
    {
      "description": "Create a folder in the focused pane, or pass pane to target the other. No name opens the naming dialog (user confirms, not MCP); a name prefills it; name + autoConfirm creates directly (errors on a name conflict).",
      "inputSchema": {
        "properties": {
          "autoConfirm": {
            "description": "With a name, create directly without the dialog. Returns once created.",
            "type": "boolean"
          },
          "name": {
            "description": "Folder name. Omit to open the dialog with the default prefill.",
            "type": "string"
          },
          "pane": {
            "description": "Target pane. Defaults to the focused pane.",
            "enum": [
              "left",
              "right"
            ],
            "type": "string"
          }
        },
        "required": [],
        "type": "object"
      },
      "name": "mkdir"
    },
    {
      "description": "Create an empty file in the focused pane, or pass pane to target the other. No name opens the naming dialog (user confirms, not MCP); a name prefills it; name + autoConfirm creates directly (errors on a name conflict).",
      "inputSchema": {
        "properties": {
          "autoConfirm": {
            "description": "With a name, create directly without the dialog. Returns once created.",
            "type": "boolean"
          },
          "name": {
            "description": "File name. Omit to open the dialog with the default prefill.",
            "type": "string"
          },
          "pane": {
            "description": "Target pane. Defaults to the focused pane.",
            "enum": [
              "left",
              "right"
            ],
            "type": "string"
          }
        },
        "required": [],
        "type": "object"
      },
      "name": "mkfile"
    },
    {
      "description": "Force a re-read of the focused pane's listing (from disk on local volumes; the watcher cache short-circuits on MTP / SMB). Use after an out-of-band change; navigation and file ops already refresh on their own.",
      "inputSchema": {
        "properties": {},
        "required": [],
        "type": "object"
      },
      "name": "refresh"
    },
    {
      "description": "Set macOS Finder color tags on files by name (else selection, else cursor). set: make the colors exactly (keeps colorless tags). toggle: flip each color. clear: remove all. macOS only; tags show in cmdr://state as [tags:red,blue].",
      "inputSchema": {
        "properties": {
          "action": {
            "description": "set | toggle | clear",
            "enum": [
              "set",
              "toggle",
              "clear"
            ],
            "type": "string"
          },
          "colors": {
            "description": "Finder color names. Required for set and toggle; ignored for clear.",
            "items": {
              "enum": [
                "red",
                "orange",
                "yellow",
                "green",
                "blue",
                "purple",
                "gray"
              ],
              "type": "string"
            },
            "type": "array"
          },
          "names": {
            "description": "Filenames to tag. Defaults to the selection, else the cursor item. Errors if a name isn't in the listing.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "pane": {
            "description": "Which pane to act in. Defaults to the focused pane.",
            "enum": [
              "left",
              "right"
            ],
            "type": "string"
          }
        },
        "required": [
          "action"
        ],
        "type": "object"
      },
      "name": "tag"
    },
    {
      "description": "Toggle whether hidden (dotfile) files show in the file lists (the showHidden flag in cmdr://state).",
      "inputSchema": {
        "properties": {},
        "required": [],
        "type": "object"
      },
      "name": "toggle_hidden"
    },
    {
      "description": "Set a pane's view mode: brief (names, only the cursor row detailed) or full (size and date on every row). full makes cmdr://state carry those details for all rows, not just the cursor.",
      "inputSchema": {
        "properties": {
          "mode": {
            "description": "View mode to set",
            "enum": [
              "brief",
              "full"
            ],
            "type": "string"
          },
          "pane": {
            "description": "Which pane to set view mode for",
            "enum": [
              "left",
              "right"
            ],
            "type": "string"
          }
        },
        "required": [
          "pane",
          "mode"
        ],
        "type": "object"
      },
      "name": "set_view_mode"
    },
    {
      "description": "Sort a pane by a field (name, ext, size, modified, created) and order (asc / desc).",
      "inputSchema": {
        "properties": {
          "by": {
            "description": "Field to sort by",
            "enum": [
              "name",
              "ext",
              "size",
              "modified",
              "created"
            ],
            "type": "string"
          },
          "order": {
            "description": "Sort order",
            "enum": [
              "asc",
              "desc"
            ],
            "type": "string"
          },
          "pane": {
            "description": "Which pane to sort",
            "enum": [
              "left",
              "right"
            ],
            "type": "string"
          }
        },
        "required": [
          "pane",
          "by",
          "order"
        ],
        "type": "object"
      },
      "name": "sort"
    },
    {
      "description": "Manage a pane's tabs: new, close, close_others, activate, set_pinned, or reopen (restore the last-closed tab). tabId defaults to the active tab where it applies; see each pane's tabs in cmdr://state.",
      "inputSchema": {
        "properties": {
          "action": {
            "description": "Action to perform on the tab",
            "enum": [
              "new",
              "close",
              "close_others",
              "activate",
              "set_pinned",
              "reopen"
            ],
            "type": "string"
          },
          "pane": {
            "description": "Which pane to operate on",
            "enum": [
              "left",
              "right"
            ],
            "type": "string"
          },
          "pinned": {
            "description": "Pin state (only for set_pinned action)",
            "type": "boolean"
          },
          "tabId": {
            "description": "Tab ID. Defaults to active tab for close, close_others, set_pinned. Required for activate. Not used for new or reopen.",
            "type": "string"
          }
        },
        "required": [
          "action",
          "pane"
        ],
        "type": "object"
      },
      "name": "tab"
    },
    {
      "description": "Open, focus, close, or confirm a dialog. Open/focus: settings, file-viewer, about, onboarding. Close: any id from cmdr://dialogs/available. confirm (token-gated) accepts an open confirmation. cmdr://state lists what's open.",
      "inputSchema": {
        "properties": {
          "action": {
            "description": "Action to perform. 'confirm' triggers the confirm button on an already-open dialog.",
            "enum": [
              "open",
              "focus",
              "close",
              "confirm"
            ],
            "type": "string"
          },
          "onConflict": {
            "description": "For confirm action on transfer-confirmation: conflict resolution policy for clashing FILES. Folders always merge (a source folder landing on a same-named dest folder merges into it), and this policy governs the files inside. Default: skip_all",
            "enum": [
              "skip_all",
              "overwrite_all",
              "rename_all"
            ],
            "type": "string"
          },
          "path": {
            "description": "For file-viewer: file path. On open without path, uses cursor file. On close without path, closes all.",
            "type": "string"
          },
          "section": {
            "description": "For settings: which section to open (e.g., 'shortcuts')",
            "type": "string"
          },
          "type": {
            "description": "Dialog type. Openable/focusable: settings, file-viewer, about, onboarding. Closable: any dialog id from cmdr://dialogs/available (also settings, file-viewer). Confirmable: transfer-confirmation (covers copy and move; 'copy-confirmation' is an alias) and delete-confirmation.",
            "type": "string"
          }
        },
        "required": [
          "action",
          "type"
        ],
        "type": "object"
      },
      "name": "dialog"
    },
    {
      "description": "Open the search dialog with optional pre-filled query and filters. If autoRun (default true), runs the search immediately. Acks once the dialog has mounted; does not wait for results to render.",
      "inputSchema": {
        "properties": {
          "autoRun": {
            "description": "Default true: open and run the search. False: open and prefill without running.",
            "type": "boolean"
          },
          "caseSensitive": {
            "description": "Case-sensitive matching",
            "type": "boolean"
          },
          "excludeSystemDirs": {
            "description": "Exclude system/build/cache folders (node_modules, .git, Caches, etc.)",
            "type": "boolean"
          },
          "isDirectory": {
            "description": "Type filter: true = folders only, false = files only, omit for both",
            "type": "boolean"
          },
          "mode": {
            "description": "Search mode. Defaults to 'ai' if AI is enabled, otherwise 'filename'.",
            "enum": [
              "ai",
              "filename",
              "regex"
            ],
            "type": "string"
          },
          "modifiedAfter": {
            "description": "ISO date string (for example, '2025-01-01')",
            "type": "string"
          },
          "modifiedBefore": {
            "description": "ISO date string",
            "type": "string"
          },
          "query": {
            "description": "Search query to pre-fill in the search bar",
            "type": "string"
          },
          "scope": {
            "description": "Scope string, same syntax as the scope chip: comma-separated paths, ! prefix for excludes",
            "type": "string"
          },
          "sizeMax": {
            "description": "Maximum file size in bytes",
            "type": "integer"
          },
          "sizeMin": {
            "description": "Minimum file size in bytes",
            "type": "integer"
          }
        },
        "required": [],
        "type": "object"
      },
      "name": "open_search_dialog"
    },
    {
      "description": "Quit the application",
      "inputSchema": {
        "properties": {},
        "required": [],
        "type": "object"
      },
      "name": "quit"
    },
    {
      "description": "Toggle focus to the other pane. Takes no parameters (a pane arg is ignored). To focus a SPECIFIC pane, use select (with count 0 to clear) or select_volume / nav_to_path on that pane, which focus it.",
      "inputSchema": {
        "properties": {},
        "required": [],
        "type": "object"
      },
      "name": "switch_pane"
    },
    {
      "description": "Swap left and right pane directories, view modes, sort orders, and selections",
      "inputSchema": {
        "properties": {},
        "required": [],
        "type": "object"
      },
      "name": "swap_panes"
    },
    {
      "description": "Search the drive index by filename pattern, size, date, or type; returns paths (no UI). Prefer over ai_search when the query is a plain pattern or filter (no LLM call), and over open_search_dialog for a programmatic lookup. Needs an indexed volume.",
      "inputSchema": {
        "properties": {
          "caseSensitive": {
            "description": "Case-sensitive matching. Default: false on macOS, true on Linux",
            "type": "boolean"
          },
          "excludeSystemDirs": {
            "description": "Exclude system/build/cache folders (node_modules, .git, Caches, etc). Default: true",
            "type": "boolean"
          },
          "limit": {
            "description": "Max results to return. Default: 30",
            "type": "integer"
          },
          "modifiedAfter": {
            "description": "ISO date, for example \"2025-01-01\"",
            "type": "string"
          },
          "modifiedBefore": {
            "description": "ISO date",
            "type": "string"
          },
          "pattern": {
            "description": "Glob or regex filename pattern (for example, \"*.pdf\", \"report*\")",
            "type": "string"
          },
          "patternType": {
            "description": "Pattern type. Default: glob",
            "enum": [
              "glob",
              "regex"
            ],
            "type": "string"
          },
          "scope": {
            "description": "Scope string: comma-separated paths, ! for excludes (for example, \"~/projects, !node_modules\")",
            "type": "string"
          },
          "sizeMax": {
            "description": "Maximum file size, human-readable",
            "type": "string"
          },
          "sizeMin": {
            "description": "Minimum file size, human-readable (for example, \"1 MB\", \"500 KB\")",
            "type": "string"
          },
          "type": {
            "description": "Filter by type. Omit for both.",
            "enum": [
              "file",
              "dir"
            ],
            "type": "string"
          }
        },
        "required": [],
        "type": "object"
      },
      "name": "search"
    },
    {
      "description": "Search with a natural-language query; the configured LLM turns it into a structured search over the drive index and returns matching paths. Use search instead when you can express the query as a pattern or filter (it skips the LLM call).",
      "inputSchema": {
        "properties": {
          "limit": {
            "description": "Max results to return. Default: 30",
            "type": "integer"
          },
          "query": {
            "description": "Natural language search query (for example, \"recent invoices marked rymd\")",
            "type": "string"
          },
          "scope": {
            "description": "Scope string: comma-separated paths, ! for excludes (for example, \"~/projects, !node_modules\"). Merged with AI-inferred scope.",
            "type": "string"
          }
        },
        "required": [
          "query"
        ],
        "type": "object"
      },
      "name": "ai_search"
    },
    {
      "description": "Set a setting value. Use the cmdr://settings resource to discover available settings and their constraints.",
      "inputSchema": {
        "properties": {
          "id": {
            "description": "Setting ID, for example 'appearance.appColor'",
            "type": "string"
          },
          "value": {
            "description": "New value for the setting"
          }
        },
        "required": [
          "id",
          "value"
        ],
        "type": "object"
      },
      "name": "set_setting"
    },
    {
      "description": "Control one volume's drive indexing. Actions: enable (on, starts first scan), disable (off, keeps DB), rescan (fresh full scan), forget (delete DB). enable/rescan return once scanning starts; poll await index_status fresh for done. See cmdr://indexing.",
      "inputSchema": {
        "properties": {
          "action": {
            "description": "enable | disable | rescan | forget",
            "enum": [
              "enable",
              "disable",
              "rescan",
              "forget"
            ],
            "type": "string"
          },
          "volumeId": {
            "description": "Volume ID to control (for example 'root', 'smb-…', 'mtp-…:1'). See cmdr://state volumes.",
            "type": "string"
          }
        },
        "required": [
          "action",
          "volumeId"
        ],
        "type": "object"
      },
      "name": "indexing"
    },
    {
      "description": "Control the operation queue: pause / resume / cancel one operationId, or pause_all / resume_all. cancel also takes operationIds (array) for several; rollback: true deletes already-copied files (single op, token-gated). See cmdr://state operations for ids.",
      "inputSchema": {
        "properties": {
          "action": {
            "description": "pause | resume | cancel | pause_all | resume_all",
            "enum": [
              "pause",
              "resume",
              "cancel",
              "pause_all",
              "resume_all"
            ],
            "type": "string"
          },
          "operationId": {
            "description": "Operation to act on (required for pause / resume / cancel unless operationIds is given). See cmdr://state operations.",
            "type": "string"
          },
          "operationIds": {
            "description": "For cancel only: several operations to cancel at once (keeps already-copied files).",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "rollback": {
            "description": "For cancel with a single operationId: delete already-copied files instead of keeping them. Requires the bearer token.",
            "type": "boolean"
          }
        },
        "required": [
          "action"
        ],
        "type": "object"
      },
      "name": "queue"
    },
    {
      "description": "Manage the user's favorites (the switcher's Favorites section). add: path (+ optional name). rename: id + name. remove: id. reorder: orderedIds, the COMPLETE new ordering. Discover ids in cmdr://state favorites.",
      "inputSchema": {
        "properties": {
          "action": {
            "description": "add | rename | remove | reorder",
            "enum": [
              "add",
              "rename",
              "remove",
              "reorder"
            ],
            "type": "string"
          },
          "id": {
            "description": "For rename / remove: the favorite id. See cmdr://state favorites.",
            "type": "string"
          },
          "name": {
            "description": "For add (optional, defaults to the path's name) / rename (required): the display label.",
            "type": "string"
          },
          "orderedIds": {
            "description": "For reorder: the complete new ordering of favorite ids.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "path": {
            "description": "For add: the folder path to favorite (~ expands to home).",
            "type": "string"
          }
        },
        "required": [
          "action"
        ],
        "type": "object"
      },
      "name": "favorites"
    },
    {
      "description": "Add a manual SMB server by address. Checks TCP reachability then adds to the host list.",
      "inputSchema": {
        "properties": {
          "address": {
            "description": "Server address: hostname, IP, IP:port, or smb:// URL",
            "type": "string"
          }
        },
        "required": [
          "address"
        ],
        "type": "object"
      },
      "name": "connect_to_server"
    },
    {
      "description": "Remove a manually-added server from the host list.",
      "inputSchema": {
        "properties": {
          "hostId": {
            "description": "Host ID to remove (for example, manual-192-168-1-100-9445)",
            "type": "string"
          }
        },
        "required": [
          "hostId"
        ],
        "type": "object"
      },
      "name": "remove_manual_server"
    },
    {
      "description": "Upgrade an OS-mounted SMB volume to a direct smb2 session for faster I/O. Uses Keychain creds. Returns OK, NeedsCredentials, or NetworkError. See cmdr://state volumes for each SMB share's smbConnectionState.",
      "inputSchema": {
        "properties": {
          "volumeId": {
            "description": "Volume ID of the SMB share (e.g. smb-192-168-1-111-445-naspi). See cmdr://state volumes.",
            "type": "string"
          }
        },
        "required": [
          "volumeId"
        ],
        "type": "object"
      },
      "name": "upgrade_smb_to_direct"
    },
    {
      "description": "Eject an ejectable volume by id (disk or MTP). Refuses honestly while an operation is reading from or writing to the volume, and for non-ejectable volumes. See cmdr://state volumes for ids.",
      "inputSchema": {
        "properties": {
          "volumeId": {
            "description": "Volume ID to eject (for example 'smb-…' or 'mtp-…:1'). See cmdr://state volumes.",
            "type": "string"
          }
        },
        "required": [
          "volumeId"
        ],
        "type": "object"
      },
      "name": "eject"
    },
    {
      "description": "Wait until a condition is met, after fire-and-forget actions or async events. Pane conditions watch a pane; index_status watches a volume's indexing freshness; operation_complete / operations_idle watch the write-operation queue.",
      "inputSchema": {
        "properties": {
          "afterGeneration": {
            "description": "Only consider state updates after this generation number. Prevents matching stale state from before an action. Get the current generation from cmdr://state or a previous await result. Pane conditions only.",
            "type": "integer"
          },
          "condition": {
            "description": "Condition to wait for: has_item / not_has_item (file list contains / no longer contains item named value — use not_has_item after a delete), item_count_gte / item_count_lte (file list has >= / <= value items), path (pane path equals value), path_contains (pane path contains value), index_status (volumeId's indexing freshness equals value: fresh / scanning / stale), operation_complete (the operation whose id is value settled — completed / cancelled / failed, reported in the result), operations_idle (no operation is running or queued; takes no value)",
            "enum": [
              "has_item",
              "not_has_item",
              "item_count_gte",
              "item_count_lte",
              "path",
              "path_contains",
              "index_status",
              "operation_complete",
              "operations_idle"
            ],
            "type": "string"
          },
          "pane": {
            "description": "Which pane to watch. Required for the pane conditions; ignored for index_status / operation_complete / operations_idle.",
            "enum": [
              "left",
              "right"
            ],
            "type": "string"
          },
          "timeoutSeconds": {
            "description": "Timeout in seconds (default 15, max 60)",
            "type": "integer"
          },
          "value": {
            "description": "Value for the condition (item name, count, path, substring, an index_status status fresh / scanning / stale, or for operation_complete the operationId). Not used by operations_idle.",
            "type": "string"
          },
          "volumeId": {
            "description": "For index_status: the volume whose indexing freshness to watch (for example 'root', 'smb-…', 'mtp-…:1').",
            "type": "string"
          }
        },
        "required": [
          "condition"
        ],
        "type": "object"
      },
      "name": "await"
    },
    {
      "description": "Navigate the focused pane to the most recently observed eligible file in ~/Downloads and select it. Errors if no eligible file exists or Cmdr lacks Full Disk Access.",
      "inputSchema": {
        "properties": {},
        "required": [],
        "type": "object"
      },
      "name": "go_to_latest_download"
    },
    {
      "description": "List past operations from the durable operation log (copy, move, delete, trash, rename, create, compress), newest first. Filter by time, item name, kind, initiator, status; paged. In-flight ops live in cmdr://state operations + the queue tool.",
      "inputSchema": {
        "properties": {
          "executionStatus": {
            "description": "Filter by lifecycle status",
            "enum": [
              "queued",
              "running",
              "done",
              "failed",
              "canceled"
            ],
            "type": "string"
          },
          "initiator": {
            "description": "Filter by who initiated the operation",
            "enum": [
              "user",
              "aiClient",
              "agent"
            ],
            "type": "string"
          },
          "kind": {
            "description": "Filter by operation kind",
            "enum": [
              "copy",
              "move",
              "delete",
              "trash",
              "rename",
              "createFolder",
              "createFile",
              "archiveEdit"
            ],
            "type": "string"
          },
          "limit": {
            "description": "Max operations to return. Default 50, max 1000.",
            "type": "integer"
          },
          "name": {
            "description": "Match operations that touched an item with this name (folded: case- and Unicode-normalized). Exact or prefix match on the item's source name (see nameMatch), NOT a substring search.",
            "type": "string"
          },
          "nameMatch": {
            "description": "How 'name' matches: exact folded-name equality, or folded-name prefix. Default: prefix.",
            "enum": [
              "exact",
              "prefix"
            ],
            "type": "string"
          },
          "offset": {
            "description": "Number of operations to skip, for paging",
            "type": "integer"
          },
          "rollbackState": {
            "description": "Filter by rollback state",
            "enum": [
              "notRollbackable",
              "rollbackable",
              "rollingBack",
              "rolledBack",
              "partiallyRolledBack"
            ],
            "type": "string"
          },
          "since": {
            "description": "Inclusive lower bound on the operation's start time (Unix milliseconds)",
            "type": "integer"
          },
          "until": {
            "description": "Inclusive upper bound on the operation's start time (Unix milliseconds)",
            "type": "integer"
          }
        },
        "required": [],
        "type": "object"
      },
      "name": "operations_list"
    },
    {
      "description": "Get one operation's header plus a page of its item rows (full source/dest paths, per-item outcome). Use after operations_list; poll this to watch a rollback settle (rollbackState leaves 'rollingBack').",
      "inputSchema": {
        "properties": {
          "limit": {
            "description": "Max item rows to return. Default 200, max 1000.",
            "type": "integer"
          },
          "offset": {
            "description": "Number of item rows to skip, for paging",
            "type": "integer"
          },
          "operationId": {
            "description": "The operation's id. The same id everywhere: from operations_list, a copy/move/delete response, cmdr://state operations, or the queue tool.",
            "type": "string"
          }
        },
        "required": [
          "operationId"
        ],
        "type": "object"
      },
      "name": "operations_get"
    },
    {
      "description": "Reverse a logged operation (delete the copies, move back, restore from trash). Rechecks each item and never overwrites; a drifted or occupied item is skipped. Returns after dispatch: poll operations_get until rollbackState leaves 'rollingBack'.",
      "inputSchema": {
        "properties": {
          "autoConfirm": {
            "description": "Must be true to roll back: a rollback writes to disk, so (like copy/move/delete) it requires the bearer token. Returns once the reversal is dispatched; poll operations_get until rollbackState leaves 'rollingBack'.",
            "type": "boolean"
          },
          "operationId": {
            "description": "The operation to reverse. Same id as operations_list, a copy/move/delete response, or cmdr://state operations.",
            "type": "string"
          }
        },
        "required": [
          "operationId"
        ],
        "type": "object"
      },
      "name": "operations_rollback"
    },
    {
      "description": "Find the user's photos by content: a scene description, text inside the image (OCR), or a tag. Returns matching file paths plus a short reason, read from the on-device index (no uploads). Omit mode to combine description + OCR. Needs image indexing on.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "limit": {
            "description": "Max hits to return (default 30, capped at 200).",
            "type": "integer"
          },
          "mode": {
            "description": "How to match. semantic: by visual description (needs the on-device model). ocr: by text recognized inside the image. tag: by a Vision object/scene tag. Omit to combine semantic + ocr (semantic leads), which is best for most 'find the photo of…' questions.",
            "enum": [
              "semantic",
              "ocr",
              "tag"
            ],
            "type": "string"
          },
          "query": {
            "description": "What to look for. In semantic mode, a natural description of the scene ('two people on a beach at sunset'); in ocr mode, words expected to appear IN the image (a receipt total, a sign, a passport field); in tag mode, a single object/scene label ('dog', 'sky').",
            "type": "string"
          },
          "volumeId": {
            "description": "Restrict to one volume id (see list_volumes / cmdr://state). Omit to search every local and SMB volume that's indexed.",
            "type": "string"
          }
        },
        "required": [
          "query"
        ],
        "type": "object"
      },
      "name": "search_photos"
    }
  ]
}
