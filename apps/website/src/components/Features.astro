---
const features = [
    {
        icon: '🧠',
        title: 'Natural language rename',
        description:
            'Type "make these lowercase and add date prefix" and watch it happen. No regex, no scripts, just words.',
        highlight: true,
    },
    {
        icon: '🔍',
        title: 'Smart search',
        description:
            'Find files by describing them: "that PDF contract from last month" or "screenshots with error messages."',
        highlight: true,
    },
    {
        icon: '⚡',
        title: 'AI batch operations',
        description: 'Organize hundreds of files with a single command. "Sort these into folders by project name."',
        highlight: true,
    },
    {
        icon: '⌨️',
        title: 'Keyboard-first',
        description: 'Navigate, select, copy, move — all without touching your mouse. Just like the old days.',
        highlight: false,
    },
    {
        icon: '🚀',
        title: 'Blazing fast',
        description: 'Built with Rust for native performance. Handles folders with 50,000+ files effortlessly.',
        highlight: false,
    },
    {
        icon: '📂',
        title: 'Two-pane layout',
        description: 'See source and destination side by side. The classic layout that just works.',
        highlight: false,
    },
]
---

<section id="features" class="relative px-6 py-32">
    <div class="mx-auto max-w-6xl">
        <!-- Section header -->
        <div class="mb-16 text-center">
            <h2 class="mb-4 text-4xl font-bold md:text-5xl">
                AI that <span class="gradient-text">actually helps</span>
            </h2>
            <p class="mx-auto max-w-2xl text-lg text-[var(--color-text-secondary)]">
                Not a chatbot. Not a gimmick. Real AI features that save you hours of tedious file work.
            </p>
        </div>

        <!-- Features grid -->
        <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
            {
                features.map((feature) => (
                    <div
                        class:list={[
                            'group rounded-2xl border p-8 transition-all duration-300',
                            feature.highlight
                                ? 'border-[var(--color-accent)]/30 bg-gradient-to-br from-[var(--color-surface)] to-[var(--color-accent)]/5 hover:border-[var(--color-accent)]/50'
                                : 'border-[var(--color-border)] bg-[var(--color-surface)] hover:border-[var(--color-accent)]/30 hover:bg-[var(--color-surface-elevated)]',
                        ]}
                    >
                        <div class="mb-4 text-4xl">{feature.icon}</div>
                        <h3 class="mb-2 text-xl font-semibold">
                            {feature.title}
                            {feature.highlight && (
                                <span class="ml-2 rounded-full bg-[var(--color-accent)]/20 px-2 py-0.5 text-xs font-medium text-[var(--color-accent)]">
                                    AI
                                </span>
                            )}
                        </h3>
                        <p class="text-[var(--color-text-secondary)]">{feature.description}</p>
                    </div>
                ))
            }
        </div>
    </div>
</section>
