---
import { dmgUrl } from '../lib/release'
import NewsletterForm from './NewsletterForm.astro'

const navLinks = [
    { href: '/#features', label: 'Features' },
    { href: '/pricing', label: 'Pricing' },
    { href: '/changelog', label: 'Changelog' },
    { href: '/roadmap', label: 'Roadmap' },
    { href: 'https://github.com/vdavid/cmdr', label: 'GitHub', external: true },
]
---

<header
    class="fixed top-0 left-0 right-0 z-50 border-b border-[var(--color-border-subtle)] bg-[var(--color-background)]/80 backdrop-blur-xl"
    transition:persist
>
    <nav class="mx-auto flex h-16 max-w-6xl items-center justify-between px-6">
        <!-- Logo -->
        <a
            href="/"
            class="flex items-center gap-2 text-xl font-semibold transition-opacity hover:opacity-80"
            aria-label="Cmdr home"
        >
            <img src="/logo-32.png" alt="" width="32" height="32" class="h-6 w-6" />
            <span>Cmdr</span>
        </a>

        <!-- Navigation -->
        <div class="flex items-center gap-8">
            <!-- Newsletter toggle -->
            <button
                type="button"
                class="cursor-pointer text-sm text-[var(--color-text-secondary)] transition-colors duration-200 hover:text-[var(--color-text-primary)]"
                aria-expanded="false"
                aria-controls="newsletter-panel"
                data-newsletter-toggle
            >
                Newsletter
            </button>

            {
                navLinks.map((link) => (
                    <a
                        href={link.href}
                        target={link.external ? '_blank' : undefined}
                        rel={link.external ? 'noopener noreferrer' : undefined}
                        class="text-sm text-[var(--color-text-secondary)] transition-colors duration-200 hover:text-[var(--color-text-primary)]"
                    >
                        {link.label}
                        {link.external && <span class="ml-1 text-xs">↗</span>}
                    </a>
                ))
            }

            <!-- CTA Button -->
            <a
                href={dmgUrl}
                class="inline-flex items-center gap-1.5 rounded-lg bg-[var(--color-accent)] px-4 py-2 text-sm font-medium text-[var(--color-background)] transition-all duration-200 hover:bg-[var(--color-accent-hover)] hover:shadow-[var(--color-accent-glow)]"
            >
                <svg class="h-4 w-4" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                    <path
                        d="M18.71 19.5c-.83 1.24-1.71 2.45-3.05 2.47-1.34.03-1.77-.79-3.29-.79-1.53 0-2 .77-3.27.82-1.31.05-2.3-1.32-3.14-2.53C4.25 17 2.94 12.45 4.7 9.39c.87-1.52 2.43-2.48 4.12-2.51 1.28-.02 2.5.87 3.29.87.78 0 2.26-1.07 3.81-.91.65.03 2.47.26 3.64 1.98-.09.06-2.17 1.28-2.15 3.81.03 3.02 2.65 4.03 2.68 4.04-.03.07-.42 1.44-1.38 2.83M13 3.5c.73-.83 1.94-1.46 2.94-1.5.13 1.17-.34 2.35-1.04 3.19-.69.85-1.83 1.51-2.95 1.42-.15-1.15.41-2.35 1.05-3.11z"
                    ></path>
                </svg>
                Download
            </a>
        </div>
    </nav>

    <!-- Newsletter slide-open panel -->
    <div id="newsletter-panel" class="newsletter-panel" aria-hidden="true">
        <div class="mx-auto flex max-w-2xl flex-col items-center gap-3 px-6 py-5">
            <p class="text-sm text-[var(--color-text-secondary)]">
                How cool that you want to hear about Cmdr news! We promise we won't spam you. ❤️
            </p>
            <div class="w-full max-w-md">
                <NewsletterForm variant="inline" />
            </div>
        </div>
    </div>
</header>

<style>
    .newsletter-panel {
        max-height: 0;
        overflow: hidden;
        transition: max-height var(--duration-slow) var(--ease-out-expo);
        border-top: 1px solid transparent;
    }

    .newsletter-panel[aria-hidden='false'] {
        max-height: 200px;
        border-top-color: var(--color-border-subtle);
    }

    @media (prefers-reduced-motion: reduce) {
        .newsletter-panel {
            transition: none;
        }
    }
</style>

<script>
    document.addEventListener('astro:page-load', () => {
        const toggle = document.querySelector<HTMLButtonElement>('[data-newsletter-toggle]')
        const panel = document.getElementById('newsletter-panel')
        if (!toggle || !panel) return

        // Skip if already bound (view transition re-runs)
        if (toggle.dataset.bound) return
        toggle.dataset.bound = 'true'

        function open() {
            panel!.setAttribute('aria-hidden', 'false')
            toggle!.setAttribute('aria-expanded', 'true')
            const emailInput = panel!.querySelector<HTMLInputElement>('input[type="email"]')
            // Delay focus until the panel animation starts
            setTimeout(() => emailInput?.focus(), 50)
        }

        function close() {
            panel!.setAttribute('aria-hidden', 'true')
            toggle!.setAttribute('aria-expanded', 'false')
        }

        toggle.addEventListener('click', () => {
            const isOpen = panel.getAttribute('aria-hidden') === 'false'
            if (isOpen) close()
            else open()
        })

        document.addEventListener('keydown', (e: KeyboardEvent) => {
            if (e.key === 'Escape' && panel.getAttribute('aria-hidden') === 'false') {
                close()
                toggle.focus()
            }
        })
    })
</script>
